VITE_API_URL=http://localhost:4000/api
VITE_GOOGLE_CLIENT_ID=

# MSG91 OTP Widget (Web) -- from the widget's dashboard "Integration" tab.
# Leave blank to fall back to a dev-only "Skip OTP" button during local
# development (never falls back in a production build).
VITE_MSG91_WIDGET_ID=
VITE_MSG91_TOKEN_AUTH=
